# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=DBopenAPI
DB_SCHEMA=openapi_schema
DB_USER=openapi_user
DB_PASSWORD=your_db_password_here
DB_ROOT_USER=postgres
DB_ROOT_PASSWORD=your_root_password_here

# Connection string built from the above parameters
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?options=-csearch_path%3D${DB_SCHEMA}

# OpenAPI configuration
# Risk API credentials - IMPORTANT: Update with actual values
OPENAPI_BASE_URL_RISK=https://api.risk.openapi.com/v1
OPENAPI_TOKEN_RISK=your_risk_api_token_here

# Company API credentials - IMPORTANT: Update with actual values
OPENAPI_BASE_URL_COMPANY=https://api.company.openapi.com/v1
OPENAPI_TOKEN_COMPANY=your_company_api_token_here

# General settings
TIMEZONE=Europe/Rome

# Debugging settings - Set to True in development to see more logs
DEBUG=True
